<h1>Vorhandene Gewässer</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @waters.each do |water| %>
  <tr>
    <td><%= link_to water.name, water %></td>
    <td><%= link_to 'Edit', edit_water_path(water) %></td>
    <td><%= link_to 'Destroy', water, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neues Gewässer', new_water_path %>
